<%= form_for(@space, multipart: true) do |f| %>
  <% if @space.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@space.errors.count, "error") %> prohibited this friend from being saved:</h2>

      <ul>
      <% @space.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :main_photo %>
    <%= f.file_field :main_photo %>
  </div>
  <div class="actions">
    <%= f.submit 'Make a space' %>
    <%= link_to 'Nevermind', root, class: 'button' %>
  </div>
<% end %>